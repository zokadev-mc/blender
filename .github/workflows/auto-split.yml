name: Auto Split Scripts

on:
  push:
    paths-ignore:
      - '**/*_partes/**' # Evita bucles infinitos: no se activa si solo cambian las partes
      - '.github/workflows/**'

permissions:
  contents: write

jobs:
  split-files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3
        with:
          fetch-depth: 2  # <--- AGREGA ESTO: Necesario para comparar "antes" y "después"

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Ejecutar script de división
        run: python autosplit.py

      - name: Commit y Push de los cambios
        run: |
          git config --global user.name "AutoSplit Bot"
          git config --global user.email "actions@github.com"
          
          # Agregamos todos los archivos generados
          git add .
          
          # Verificamos si hay cambios reales para evitar commits vacíos
          if ! git diff-index --quiet HEAD; then
            git commit -m "Auto-split: Actualización de partes de archivos"
            
            # Sincronizamos con el remoto antes de subir
            # Esto resuelve el error 'fetch first' aplicando tus cambios sobre los remotos
            git pull --rebase origin main
            
            git push origin main
          else
            echo "No se detectaron cambios, nada que subir."
          fi
